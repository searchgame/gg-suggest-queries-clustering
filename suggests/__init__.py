"""Google Suggest scraping, embedding and clustering.

Nothing in this package imports Streamlit. The UI lives in ``app.py`` and is
the only place caching and widgets appear, so every module here can be driven
from a script or a test.
"""

__version__ = "3.0.0"
