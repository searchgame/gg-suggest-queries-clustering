# Pinned to what the local environment already provides.
# numpy is held below 2.0: the installed hdbscan 0.8.44 wheel is compiled
# against the numpy 1.x ABI and segfaults or fails to import under numpy 2.
numpy<2
pandas>=2.0,<3
requests>=2.31
streamlit>=1.32,<2
plotly>=5.20
scikit-learn>=1.3
umap-learn>=0.5.5
hdbscan>=0.8.40

# Optional: enables the XLSX export button. Everything else works without it.
# openpyxl>=3.1

# Development only.
# pytest>=7.4
